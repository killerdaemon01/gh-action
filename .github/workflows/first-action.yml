name: First Workflow
#manually starts the workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "Hello world!"
      - name: Print Goodbye
        run: echo "Done - Bye!"
